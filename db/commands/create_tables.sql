CREATE TABLE user_info (
    id INTEGER PRIMARY KEY,
    email TEXT,
    password TEXT
);

CREATE TABLE staff_info (
    id INTEGER PRIMARY KEY,
    firtst_name TEXT,
    surname TEXT,
    email TEXT,
    password TEXT,
    role TEXT
);